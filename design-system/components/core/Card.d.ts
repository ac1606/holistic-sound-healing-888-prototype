/**
 * @startingPoint section="Components" subtitle="Soft-shadow surface for testimonials, services, freebies" viewport="700x220"
 */
export interface CardProps {
  children: React.ReactNode;
  padding?: string;
}
