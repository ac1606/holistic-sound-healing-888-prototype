Primary call-to-action button for the HSH 888 site — used for "Book a Consultation", form submits, etc.

```jsx
<Button variant="primary" size="md" onClick={() => {}}>Book a Consultation</Button>
```

Variants: `primary` (warm gold→orange gradient, main CTA), `secondary` (cool teal→blue gradient, alternate action), `ghost` (outlined, for secondary nav-level actions), `dark` (solid navy, for use on light/cream sections needing more contrast). Sizes: `sm`, `md`, `lg`.
