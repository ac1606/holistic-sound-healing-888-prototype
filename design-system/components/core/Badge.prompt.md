Small pill label for session types, statuses, or highlights ("Group Session", "New").

```jsx
<Badge tone="warm">Individual Session</Badge>
```

Tones: `warm`, `cool`, `navy` — match to context (warm = featured, cool = informational, navy = neutral/default).
